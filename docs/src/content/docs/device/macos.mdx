---
title: macOS
description: How to install Certificate and debug HTTPS contents in macOS device.
---

import { Image } from 'astro:assets';
import certImage from './src/assets/btn-security.svg';


How to install Certificate and debug HTTPS request/response on macOS device.

## Install Certificates on macOS

In order to capture encrypted HTTPS messages (request/response), 
you have to install **EchoProxy CA Certificate** on your current machine.

You can install the EchoProxy CA Certificate by navigating to
* Toolbar icon <Image src={certImage} class="flex-inline" alt="Certificate" />

